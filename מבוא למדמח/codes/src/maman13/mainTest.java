package maman13;

public class mainTest {

    public static void main(String[] args) {
        /* Square3x3 square3x1 = new Square3x3();
        int counter = 1;
        for (int i = 0; i < 3; i++) {
            for (int j = 0; j < 3; j++) {
                square3x1.setXY(i, j, counter);
                counter++;
            }
        }
        boolean[] values = new boolean[10];
        square3x1.setXY(1, 2, 9);
        square3x1.whosThereRow(1, values);
        for (int i = 0; i < 10; i++) {
            System.out.println(i + " : " + values[i]);
        }
        System.out.println(square3x1.allThere());*/

    }
}
